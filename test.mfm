# Hello World

This is a [Misskey Flavored Markdown](https://misskey-hub.net/en/docs/for-users/features/mfm/) parsing test file!

> quote
> this is a quote block
> a
> **bold**
>> nesting
>> blockquote

Formatting:

**BOLD**

*italic*

~~strikeout~~

<small>small</small>

<center>centered</center>

BBBBBB

$[ruby **Miss***key* ミス**キー**] 

Links: https://www.block36.gay <- an inline link

[a link with custom text](https://www.block36.gay)

### Codeblocks

`inline **code**`

```js
//codeblock (js)
function test(a) {
  console.log("some codeblock")
}
```

```rs
//other lang test
pub fn something(&self) -> usize {
  println!("{}", self.0);
  30
}
```

```racket
(define (main b c)
  (println (+ b c)))
```

### Flip

$[flip Flip horizontally]
$[flip.v Flip vertically]
$[flip.h,v Flip both ways]

### Font

$[font.serif serif]
$[font.monospace monospace]

### Blur

$[blur Blurred text]

### Search 

A very minimal implementation is done here to allow users to customize what the search button should do.


Search something search

### Colors

$[fg.color=f00 red]
$[bg.color=ff0 yellow]
$[bg.color=3353ef $[fg.color=aafe77 *something* **else**]]

### Border

$[border.style=solid,width=4 Default]

$[border.style=hidden No border]

$[border.style=dotted,width=2 Dotted]

$[border.style=dashed,width=2 Dashed]

$[border.style=double,width=4 Double]

$[border.style=groove,width=4 Embossed A]

$[border.style=ridge,width=4 Embossed B]

$[border.style=inset,width=4 Inset A]

$[border.style=outset,width=4 Inset B]

$[border.color=d00 Border color]

$[border.width=5 Border width]

$[border.radius=6,width=2 Border radius]

$[border.radius=5,width=2,color=888 $[position.x=1.5 ＣＳＳ]

$[position.x=1.5 完全に理解した]]

$[border.radius=5,width=2,color=888,noclip $[position.x=1.5 ＣＳＳ]

$[position.x=1.5 完全に理解した]]

# Markdown Extras

### Lists

- unordered list
- bbbbb
- λ
  - indent

1. ordered list
2. number two
  1. sublist

### Horizontal Rule

---

a


